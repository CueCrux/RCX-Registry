{
  "corpus": "SKILLS-32",
  "note": "One query per skill in the vendored skills-mcp corpus. Written as a task someone would actually type, deliberately avoiding the skill's own description wording — a query set copied from the descriptions measures string overlap, not retrieval.",
  "queries": [
    { "q": "how do I build a spreadsheet with formulas and charts from python", "expect": "xlsx-creator" },
    { "q": "turn these survey results into summary statistics and a chart", "expect": "data-analysis" },
    { "q": "our CI should run the test suite on every pull request", "expect": "github-actions" },
    { "q": "customer wants to pay monthly instead of a one off charge", "expect": "stripe-integration" },
    { "q": "extract the tables out of a scanned invoice document", "expect": "pdf-processing" },
    { "q": "build a slide deck from this outline", "expect": "pptx-creator" },
    { "q": "the join between orders and customers is slow", "expect": "sql-query-writer" },
    { "q": "stop tenants reading each other's rows in postgres", "expect": "supabase-integration" },
    { "q": "spin up the same cloud environment from code", "expect": "terraform" },
    { "q": "package my node app so it runs the same everywhere", "expect": "docker-containerization" },
    { "q": "we need a consistent commit message format for the team", "expect": "git-commit-writer" },
    { "q": "our project landing page on github is empty", "expect": "readme-writer" },
    { "q": "the model keeps ignoring my instructions, how should I structure it", "expect": "llm-prompt-engineering" },
    { "q": "add tool calling to my anthropic integration", "expect": "claude-api" },
    { "q": "switch our assistant over to google's models", "expect": "gemini-api" },
    { "q": "stream tokens back to the browser from gpt", "expect": "openai-api" },
    { "q": "expose my internal tools to an agent client", "expect": "mcp-server-builder" },
    { "q": "content flashes before data loads with the app router", "expect": "nextjs-best-practices" },
    { "q": "this component re-renders far too often", "expect": "react-best-practices" },
    { "q": "type a function whose return shape depends on a discriminant", "expect": "typescript-patterns" },
    { "q": "reactive counter using the composition api", "expect": "vue-framework" },
    { "q": "our landing page looks like every other generic AI site", "expect": "frontend-design" },
    { "q": "grab all the product prices off this site including page two", "expect": "web-scraper" },
    { "q": "generate a contract letter as a word file", "expect": "docx-creator" },
    { "q": "one flexible query endpoint instead of twenty rest routes", "expect": "graphql-api" },
    { "q": "handle oauth refresh tokens when calling a third party service", "expect": "api-integration" },
    { "q": "deploy a function to the edge with key value storage", "expect": "cloudflare-workers" },
    { "q": "look over this diff before I merge it", "expect": "code-review" },
    { "q": "add pytest coverage for the auth module", "expect": "test-writer" },
    { "q": "models, admin views and an ORM for a python web app", "expect": "django-web-framework" },
    { "q": "async python api with pydantic request validation", "expect": "fastapi" },
    { "q": "make an interactive dashboard as one self contained html file", "expect": "web-artifacts-builder" }
  ]
}
