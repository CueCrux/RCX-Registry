# Independent re-implementation (gate evidence — non-normative)

`reimpl.py` was written by an implementer (GPT-5.6) whose workspace contained ONLY `spec/v1/*.md` + `spec/v1/vectors/` — no access to the Rust reference implementation. It re-implements the canonical CBOR/JSON encoders, hashing, receipt construction, signature verification, the snapshot set digest, and chain checks from the spec text alone, then reproduces every conformance vector: **292/292 computable fields, including 6/6 chain receipts minted independently from structured inputs (36/36 mint stages)**. `REPORT.md` is its full run report incl. the ambiguity-log history. This directory is evidence for the M0 gate of ExecPlan rcx-registry-v11-adoption-trust-hardening-2026-07-19; it is not part of the normative spec.
