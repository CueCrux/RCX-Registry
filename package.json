{
  "name": "@cuecrux/rcx-registry",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.12.1",
  "scripts": {
    "check": "cargo check --workspace",
    "fmt": "cargo fmt --all",
    "fmt:check": "cargo fmt --all --check",
    "test": "cargo test --workspace"
  }
}
